<%= form_with(model: family) do |form| %>
  <% if family.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(family.errors.count, "error") %> prohibited this family from being saved:</h2>

      <ul>
        <% family.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <p>Название: <%= form.text_field :name %></p>
    <p>Игровой персонаж: <%= form.select :player_ids, options_for_select(Player.all.collect{ |p| [p.name, p.id] }, family.player_ids), {include_blank: true}, class: 'form-select', style: "width: 200px" %></p>
    <%= form.submit "Отправить" %>
  </div>
<% end %>
