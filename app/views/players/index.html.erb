<p style="color: green"><%= notice %></p>

<h1>Players</h1>

<div id="players">
  <% @players.each do |player| %>
    <% link = "/players/#{player.id}" %>
    <p>
      <%= link_to player.name, link %>
    </p>
  <% end %>
</div>

<%= link_to "New player", new_player_path %>
