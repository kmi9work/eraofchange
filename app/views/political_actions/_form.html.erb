<%= form_with(model: political_action) do |form| %>
  <% if political_action.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(political_action.errors.count, "error") %> prohibited this political_action from being saved:</h2>

      <ul>
        <% political_action.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :year, style: "display: block" %>
    <%= form.number_field :year %>
  </div>

  <div>
    <%= form.label :success, style: "display: block" %>
    <%= form.number_field :success %>
  </div>

  <div>
    <%= form.label :params, style: "display: block" %>
    <%= form.text_field :params %>
  </div>

  <div>
    <%= form.label :political_action_type_id, style: "display: block" %>
    <%= form.text_field :political_action_type_id %>
  </div>

  <div>
    Игровой персонаж: <%= form.select :player_id, options_for_select(Player.all.collect{ |p| [p.name, p.id] }, political_action.player_id), {:include_blank => true}, class: 'form-select', style: "width: 200px" %>
  </div>

   <div>
    <%= form.submit %>
  </div>
<% end %>
