<p style="color: green"><%= notice %></p>

<h2>Гильдия: <%= @guild.name %></h2>

Игровые персонажи в гильдии:<br>
<ul>
  <% @guild.players.each do |player| %>
    <li><%= link_to player.name, player %></li>
  <% end -%>
</ul>

Предприятия у гильдии:<br>
<ul>
  <% @guild.plants.each do |plant| %>
    <li><%= link_to plant.name, plant %></li>
  <% end -%>
</ul>

<hr>

<div>
  <%= link_to "Редактировать", edit_guild_path(@guild) %> |
  <%= link_to "Все гильдии", guilds_path %>
  <%= button_to "Удалить", @guild, method: :delete %>
</div>
