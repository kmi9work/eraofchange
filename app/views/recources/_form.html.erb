<%= form_with(model: recource) do |form| %>
  <% if recource.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recource.errors.count, "error") %> prohibited this recource from being saved:</h2>

      <ul>
        <% recource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <p>Название: <%= form.text_field :name %></p>
    <p>Стоимость: <%= form.text_field :price %></p>
    <%= form.submit 'Отправить' %>
  </div>
<% end %>
