<%= form_with(model: ideologist_type) do |form| %>
  <% if ideologist_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ideologist_type.errors.count, "error") %> prohibited this ideologist_type from being saved:</h2>

      <ul>
        <% ideologist_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
