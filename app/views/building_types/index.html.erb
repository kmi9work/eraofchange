<p style="color: green"><%= notice %></p>

<h1>Типы построек</h1>
<%= link_to "Создать новый тип постройки", new_building_type_path, class: 'btn' %>
 <table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Тип постройки</th>
      <th scope="col">Действия</th>
    </tr>
  </thead>
  <tbody>
    <% @building_types.each do |building_type| %>

     <tr>
      <td scope="row"><%= building_type.id %></td>
      <td><%= link_to building_type.title, building_type_path(building_type) %></td>
      <td><%= link_to "Редактировать тип постройки", edit_building_type_path(building_type), class: 'btn1' %>
          <%= button_to "Удалить тип постройки", building_type_path(building_type), method: :delete, class: 'btn2', id: "delete-#{building_type.id}"%></td>
        </ul>
      </td>
     </tr>
    <% end -%>
  </tbody>
</table>

<hr>


