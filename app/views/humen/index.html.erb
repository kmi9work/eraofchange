<p style="color: green"><%= notice %></p>

<h1>Люди</h1>
<%= link_to "Создать нового человека", new_human_path, class: 'btn' %>
<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Имя</th>
      <th scope="col">Действия</th>
    </tr>
  </thead>
  <tbody>
    <% @humen.each do |human| %>
    <tr>
      <td scope="row"><%= human.id %></td>
      <td><%= link_to human.name, human_path(human) %></td>
      <td><%= link_to "Редактировать человека", edit_human_path(human), class: 'btn1' %>
          <%= button_to "Удалить человека", human_path(human), method: :delete, class: 'btn2', id: "delete"%></td>
         </ul>
      </td>
     </tr>
    <% end -%>
  </tbody>
</table>

<hr>


