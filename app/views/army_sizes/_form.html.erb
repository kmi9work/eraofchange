<%= form_with(model: army_size) do |form| %>
  <% if army_size.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(army_size.errors.count, "error") %> prohibited this army_size from being saved:</h2>

      <ul>
        <% army_size.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :level, style: "display: block" %>
    <%= form.number_field :level %>
  </div>

  <div>
    <%= form.label :params, style: "display: block" %>
    <%= form.text_field :params %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
