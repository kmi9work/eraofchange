<%= form_with(model: player_type) do |form| %>
  <% if player_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(player_type.errors.count, "error") %> prohibited this player_type from being saved:</h2>

      <ul>
        <% player_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    Название: <%= form.text_field :name %>
  </div>

  <div>
    Игровой персонаж: <%= form.select :player_ids, options_for_select(Player.all.collect{ |p| [p.name, p.id] }, player_type.player_ids), {include_blank: true}, {multiple: true, class: 'form-select', style: "width: 200px"} %>
  </div>

  <div>
    <%= form.submit 'Отправить' %>
  </div>
<% end %>
